# The shared password for the site. Leave it unset and the site is wide open.
RECIPES_PASSWORD=

# From the Vercel Blob store. Get it locally with `vercel env pull .env.local`
# after linking a Blob store to the project.
BLOB_READ_WRITE_TOKEN=
